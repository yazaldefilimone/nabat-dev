const from = document.querySelector('from');


alert (from);


export const isValid = (props) => {
    if (!props){
        return false;
    }

    /*    forEasch(item => {
        if (!item){
            return false;
        }
    });*/
}
